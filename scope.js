function sum(num1,num2){

    const result= num1+num2;
    return result;
}
const output = sum(10,10);
console.log(output);
console.log(result); // its not a global variable...if we declare var then it hoisting ta variable in up...and its scope in the parents access area...

